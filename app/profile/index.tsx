import {ThemedText, ThemedView} from "@/components";

const ProfilePage = () => {
  return(
      <ThemedView className={'flex-1 items-center justify-center'}>
        <ThemedText >profile page2</ThemedText>
      </ThemedView>
  )
}
export default ProfilePage
