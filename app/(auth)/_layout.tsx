import {ThemedText, ThemedView} from "@/components";

const AuthLayout = () => {
  return(
      <ThemedView>
          <ThemedText>auth layout</ThemedText>
      </ThemedView>
  )
}
export default AuthLayout
